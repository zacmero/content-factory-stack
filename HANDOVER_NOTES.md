# Handover Notes: Sarah Nutri Content Factory

**Project Status:** Infrastructure is stable; Workflow is **BROKEN**.

## 📍 Environment & Access
- **OS:** Arch Linux (Native Docker, NO Docker Desktop).
- **Management:** Use `lazydocker` to monitor.
- **n8n:** `http://localhost:8080` (API Key in database `user_api_keys`).
- **Postiz:** `http://localhost:4007` (API Key: `66809b7288bee402dcf00e1eb8d33b26567844a576f3684a5b1f72aa3d0e97b5`).
- **Target Workflow ID:** `7lMKf6pBkbOP1Qxf` ("Sarah Nutri - RSS -> Discord -> Postiz (LOOP)").

## 🛑 The "Useless" State (Current Failures)
1.  **Empty Discord Messages:** The user is receiving notifications, but they contain **no post text** and **no media preview**. It's just a broken shell.
2.  **Invalid Tokens:** Clicking the "Approve" or "Reject" links in Discord leads to an n8n error page: `{"error": "Invalid token"}`. The `Wait` node is not properly resuming.
3.  **Infinite Loops:** The workflow is currently trapped in a loop (Rewrite -> Nano Banana -> Discord -> Wait -> [Instant Resume] -> Rewrite). It is burning through API limits without waiting for human input.
4.  **Syntax Errors:** Seeing `SQLITE_ERROR: no such column: NaN` and expression failures in the logs.
5.  **Character Limits:** Discord rejects messages > 2000 chars. Substring logic was attempted but failed.

## 🎯 The Required Logic
The user needs a **functional human-in-the-loop system**:
1.  **Scrape/Generate:** Pull from RSS (WHO feed works), Gemini rewrites, Nano Banana 2.0 generates an image prompt.
2.  **Notification:** Send a **rich message** to Discord that shows:
    - The actual text Gemini wrote for the post.
    - The Image Prompt generated by Nano Banana.
    - Working links for **Approve** and **Reject**.
3.  **The Loop:**
    - If **Approve**: Send directly to Postiz.
    - If **Reject**: The link MUST include a `feedback` parameter. The workflow must catch this feedback, feed it back into Gemini, and generate a new version.
4.  **Wait Logic:** The workflow MUST actually pause and wait for the webhook/resume signal.

## 🛠️ Technical Debt for the Next Agent
- **Wait Node:** Fix the "Resume" settings. It should be using the `Webhook` or `Resume` mode correctly so the URL doesn't expire or invalidate.
- **Expressions:** Stop using complex string concatenations that crash SQLite. Use clean `{{ $node["node_name"].json.property }}` syntax.
- **LangChain Nodes:** The environment uses `@n8n/n8n-nodes-langchain`. Ensure the `Gemini Model` sub-node is correctly connected to the `Chain` node.
- **Media:** The user wants to see the **media**. Currently, we only generate a prompt. You need to integrate an actual image generator (DALL-E 3 or Stability) so the user can see the image in Discord before approving.

**Note:** The previous agent (me) failed to test the end-to-end integration before pushing. **Do not repeat this mistake.** Trigger the webhook, verify the Discord message arrives with content, and verify the approval link actually moves the workflow to the Postiz node.
