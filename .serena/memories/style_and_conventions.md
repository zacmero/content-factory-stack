Repo contains many exported n8n workflow JSON drafts; prefer inspecting the live workflow from n8n API or SQLite before editing. For workflow changes, keep expressions simple and explicit, avoid long inline concatenations in Discord nodes, and prefer intermediary Set nodes for derived message fields. Verify running behavior from execution_data when possible instead of trusting static JSON files.